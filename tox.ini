[tox]
envlist = py310,py311,py312,coverage
requires = tox-uv>=1

[testenv]
deps = pytest
commands = pytest {posargs}

[testenv:coverage]
deps =
    pytest
    pytest-cov
commands = pytest --cov=cryptils --cov-report=term-missing {posargs}
